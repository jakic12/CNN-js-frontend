/* eslint no-restricted-globals: 1 */

export default `
const { CNN } = require("../CNN-js/cnn");

export function trainNetwork(network) {
  console.log(network);
  const a = new CNN();
  return "a";
}
`;
